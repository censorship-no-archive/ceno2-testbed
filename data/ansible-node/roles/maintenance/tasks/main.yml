---

# Accept only `IPv4:PORT` or `[IPv6]:PORT` lines with a numeric PORT.
- name: Fix peer locator's test peer list file in case it contains bogus entries
  command: sed -Ei '/^([.0-9]+|\[[:0-9a-fA-F]+\]):[0-9]+$/!d' /root/peer_list.txt
  ignore_errors: true
